spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: root

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 9000